# Notes
Conversion from yaml to json was done with CLI command `yj` for parsing 2.6.0 Nifi OpenAPI specification. 

On the conversion 4 yaml errors were found and manually corrected related to `? ...` string patterns. 
